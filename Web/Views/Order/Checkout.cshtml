@using Xenon.Web.Models
@model CheckoutViewModel
@{
    Layout = "_CheckoutLayout";
    ViewData["Title"] = "Checkout";
    var subtotal = Model.Subtotal;
    var isAuth = Model.IsAuthenticated;

    string DeliveryPriceText(DeliveryOption option)
    {
        if (option.Price == 0) return "free";
        if (option.FreeFrom.HasValue)
            return $"{option.Price.ToString("c")} · free from {option.FreeFrom.Value.ToString("c")}";
        return option.Price.ToString("c");
    }
}

<h2 class="mb-3">Checkout</h2>

<form asp-action="Checkout" method="post">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="All" class="text-danger mb-3"></div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card checkout-card mb-3">
                <div class="card-header"><i class="fas fa-user me-1"></i> Contact details</div>
                <div class="card-body">
                    @if (isAuth && Model.UserProfile != null)
                    {
                        <div class="alert alert-info mb-3">
                            <i class="fas fa-info-circle me-1"></i>
                            Данные заполнены из вашего профиля.
                            <a href="/Profile/Settings" class="alert-link">Изменить в настройках</a>
                        </div>
                        <input type="hidden" asp-for="Order.Name" />
                        <input type="hidden" asp-for="Order.Phone" />
                        <input type="hidden" asp-for="Order.Email" />
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label">Full name</label>
                                <input class="form-control" value="@Model.Order.Name" readonly />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label">Email</label>
                                <input class="form-control" value="@Model.Order.Email" readonly />
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Phone</label>
                            <input class="form-control" value="@Model.Order.Phone" readonly />
                        </div>
                    }
                    else
                    {
                        <div class="mb-3">
                            <label asp-for="Order.Name" class="form-label"></label>
                            <input asp-for="Order.Name" class="form-control" placeholder="Recipient full name" />
                            <span asp-validation-for="Order.Name" class="text-danger"></span>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Order.Phone" class="form-label"></label>
                                <input asp-for="Order.Phone" class="form-control" placeholder="Your phone number" />
                                <span asp-validation-for="Order.Phone" class="text-danger"></span>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="Order.Email" class="form-label"></label>
                                <input asp-for="Order.Email" class="form-control" placeholder="you@example.com" />
                                <span asp-validation-for="Order.Email" class="text-danger"></span>
                            </div>
                        </div>
                    }
                </div>
            </div>

            <div class="card checkout-card mb-3">
                <div class="card-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <span><i class="fas fa-map-marker-alt me-1"></i> Delivery address</span>
                        @if (isAuth && !string.IsNullOrEmpty(Model.UserProfile?.Address))
                        {
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="useDifferentAddress"
                                       onchange="toggleAddressFields()" />
                                <label class="form-check-label" for="useDifferentAddress">
                                    Другой адрес
                                </label>
                            </div>
                        }
                    </div>
                </div>
                <div class="card-body">
                    @if (isAuth && Model.UserProfile != null)
                    {
                        <div id="profileAddress">
                            <input type="hidden" asp-for="Order.Country" />
                            <input type="hidden" asp-for="Order.City" />
                            <input type="hidden" asp-for="Order.Street" />
                            <input type="hidden" asp-for="Order.Building" />
                            <input type="hidden" asp-for="Order.Apartment" />
                            <input type="hidden" asp-for="Order.PostalCode" />
                            <div class="alert alert-success">
                                <i class="fas fa-map-marker-alt me-1"></i>
                                @Model.UserProfile.Country, @Model.UserProfile.City, @Model.UserProfile.Address
                            </div>
                        </div>
                        <div id="customAddress" style="display:none;">
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <label asp-for="Order.Country" class="form-label"></label>
                                    <input asp-for="Order.Country" class="form-control" placeholder="Russia" />
                                    <span asp-validation-for="Order.Country" class="text-danger"></span>
                                </div>
                                <div class="col-md-8 mb-3">
                                    <label asp-for="Order.City" class="form-label"></label>
                                    <input asp-for="Order.City" class="form-control" placeholder="Moscow" />
                                    <span asp-validation-for="Order.City" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label asp-for="Order.Street" class="form-label"></label>
                                    <input asp-for="Order.Street" class="form-control" placeholder="Street address" />
                                    <span asp-validation-for="Order.Street" class="text-danger"></span>
                                </div>
                                <div class="col-md-3 mb-3">
                                    <label asp-for="Order.Building" class="form-label"></label>
                                    <input asp-for="Order.Building" class="form-control" placeholder="House number" />
                                    <span asp-validation-for="Order.Building" class="text-danger"></span>
                                </div>
                                <div class="col-md-3 mb-3">
                                    <label asp-for="Order.Apartment" class="form-label"></label>
                                    <input asp-for="Order.Apartment" class="form-control" placeholder="Apartment" />
                                    <span asp-validation-for="Order.Apartment" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <label asp-for="Order.PostalCode" class="form-label"></label>
                                    <input asp-for="Order.PostalCode" class="form-control" placeholder="123456" />
                                    <span asp-validation-for="Order.PostalCode" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label asp-for="Order.Country" class="form-label"></label>
                                <input asp-for="Order.Country" class="form-control" placeholder="Russia" />
                                <span asp-validation-for="Order.Country" class="text-danger"></span>
                            </div>
                            <div class="col-md-8 mb-3">
                                <label asp-for="Order.City" class="form-label"></label>
                                <input asp-for="Order.City" class="form-control" placeholder="Moscow" />
                                <span asp-validation-for="Order.City" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Order.Street" class="form-label"></label>
                                <input asp-for="Order.Street" class="form-control" placeholder="Street address" />
                                <span asp-validation-for="Order.Street" class="text-danger"></span>
                            </div>
                            <div class="col-md-3 mb-3">
                                <label asp-for="Order.Building" class="form-label"></label>
                                <input asp-for="Order.Building" class="form-control" placeholder="House number" />
                                <span asp-validation-for="Order.Building" class="text-danger"></span>
                            </div>
                            <div class="col-md-3 mb-3">
                                <label asp-for="Order.Apartment" class="form-label"></label>
                                <input asp-for="Order.Apartment" class="form-control" placeholder="Apartment" />
                                <span asp-validation-for="Order.Apartment" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label asp-for="Order.PostalCode" class="form-label"></label>
                                <input asp-for="Order.PostalCode" class="form-control" placeholder="123456" />
                                <span asp-validation-for="Order.PostalCode" class="text-danger"></span>
                            </div>
                        </div>
                    }

                    @await Html.PartialAsync("_CheckoutMap", Model.Order)
                </div>
            </div>

            <div class="card checkout-card mb-3">
                <div class="card-header"><i class="fas fa-truck me-1"></i> Delivery method</div>
                <div class="card-body">
                    @foreach (var option in Model.DeliveryOptions)
                    {
                        <div class="form-check checkout-option">
                            <input type="radio" class="form-check-input"
                                   asp-for="Order.DeliveryMethod" value="@option.Method"
                                   id="delivery-@((int)option.Method)"
                                   data-price="@option.Price.ToString("0.00", System.Globalization.CultureInfo.InvariantCulture)"
                                   data-free-from="@(option.FreeFrom?.ToString("0.00", System.Globalization.CultureInfo.InvariantCulture))" />
                            <label class="form-check-label" for="delivery-@((int)option.Method)">
                                <b>@option.Name</b> <span class="text-primary">— @DeliveryPriceText(option)</span>
                                <div class="text-muted small">@option.Description</div>
                            </label>
                        </div>
                    }
                </div>
            </div>

            <div class="card checkout-card mb-3">
                <div class="card-header"><i class="fas fa-credit-card me-1"></i> Payment method</div>
                <div class="card-body">
                    @foreach (var option in Model.PaymentOptions)
                    {
                        <div class="form-check checkout-option">
                            <input type="radio" class="form-check-input"
                                   asp-for="Order.PaymentMethod" value="@option.Method"
                                   id="payment-@((int)option.Method)" />
                            <label class="form-check-label" for="payment-@((int)option.Method)">
                                <b>@option.Name</b>
                                <div class="text-muted small">@option.Description</div>
                            </label>
                        </div>
                    }
                </div>
            </div>

            <div class="card checkout-card mb-3">
                <div class="card-header"><i class="fas fa-comment me-1"></i> Order comment</div>
                <div class="card-body">
                    <textarea asp-for="Order.Comment" class="form-control" rows="3"
                              placeholder="Comment (optional)"></textarea>
                    <span asp-validation-for="Order.Comment" class="text-danger"></span>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card checkout-card checkout-summary">
                <div class="card-header"><i class="fas fa-shopping-bag me-1"></i> Your order</div>
                <div class="card-body">
                    <ul class="list-group list-group-flush mb-3">
                        @foreach (var line in Model.Cart.Lines)
                        {
                            <li class="list-group-item d-flex justify-content-between align-items-start px-0">
                                <span class="me-2">
                                    @line.Product.Name
                                    <span class="text-muted small d-block">× @line.Quantity</span>
                                </span>
                                <span>@((line.Product.Price * line.Quantity).ToString("c"))</span>
                            </li>
                        }
                    </ul>
                    <div class="d-flex justify-content-between mb-1">
                        <span>Subtotal</span>
                        <span>@subtotal.ToString("c")</span>
                    </div>
                    <div class="d-flex justify-content-between mb-1">
                        <span>Delivery</span>
                        <span id="summary-shipping">@Model.ShippingCost.ToString("c")</span>
                    </div>
                    <hr />
                    <div class="d-flex justify-content-between fw-bold fs-5">
                        <span>Total</span>
                        <span id="summary-total">@Model.Total.ToString("c")</span>
                    </div>
                </div>
                <div class="card-footer bg-white">
                    <button type="submit" class="btn btn-primary btn-lg w-100">
                        <i class="fas fa-check me-1"></i> Place order
                    </button>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/dist/jquery.validate.unobtrusive.min.js"></script>
    <script src="~/js/checkout-map.js"></script>
    <script>
        function toggleAddressFields() {
            var checkbox = document.getElementById('useDifferentAddress');
            var profileAddr = document.getElementById('profileAddress');
            var customAddr = document.getElementById('customAddress');
            if (checkbox && profileAddr && customAddr) {
                if (checkbox.checked) {
                    profileAddr.style.display = 'none';
                    customAddr.style.display = 'block';
                } else {
                    profileAddr.style.display = 'block';
                    customAddr.style.display = 'none';
                }
            }
        }
        (function () {
            var subtotal = @Model.Subtotal.ToString("0.00", System.Globalization.CultureInfo.InvariantCulture);
            var radios = document.querySelectorAll('input[name="Order.DeliveryMethod"]');

            function format(value) {
                return new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(value);
            }

            function updateSummary() {
                var selected = document.querySelector('input[name="Order.DeliveryMethod"]:checked');
                if (!selected) return;
                var price = parseFloat(selected.dataset.price || '0');
                var freeFrom = selected.dataset.freeFrom
                    ? parseFloat(selected.dataset.freeFrom)
                    : null;
                var shipping = (freeFrom !== null && subtotal >= freeFrom) ? 0 : price;
                document.getElementById('summary-shipping').textContent = format(shipping);
                document.getElementById('summary-total').textContent = format(subtotal + shipping);
            }

            radios.forEach(function (radio) {
                radio.addEventListener('change', updateSummary);
            });
            updateSummary();
        })();
    </script>
}
