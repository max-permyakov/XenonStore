@page
@using Xenon.Web.Models
@model CompletedModel
@{
    Layout = "_CheckoutLayout";
    ViewData["Title"] = "Заказ оформлен";
    var order = Model.Order;
    var delivery = order != null
        ? OrderCheckoutOptions.DeliveryOptions.First(o => o.Method == order.DeliveryMethod).Name
        : null;
    var payment = order != null
        ? OrderCheckoutOptions.PaymentMethodName(order.PaymentMethod)
        : null;
    var status = order != null
        ? OrderCheckoutOptions.PaymentStatusName(order.PaymentStatus)
        : null;
}

@if (order == null)
{
    <div class="text-center py-5">
        <h2>Заказ не найден</h2>
        <p class="text-muted">Возможно, ссылка устарела.</p>
        <a class="btn btn-primary" asp-controller="Home" asp-action="Index">
            <i class="fas fa-store me-1"></i> Вернуться в магазин
        </a>
    </div>
}
else
{
    <div class="mx-auto" style="max-width: 760px;">
        <div class="text-center mb-4">
            <i class="fas fa-check-circle text-success" style="font-size: 3rem;"></i>
            <h2 class="mt-2">Спасибо за заказ!</h2>
            <p class="text-muted mb-1">
                Заказ №@order.OrderID оформлен @order.OrderDate.ToLocalTime().ToString("g")
            </p>
            <p class="text-muted small mb-0">
                Оплата: @payment · <span class="badge bg-secondary">@status</span>
            </p>
        </div>

        <div class="card checkout-card mb-3">
            <div class="card-header"><i class="fas fa-shopping-bag me-1"></i> Состав заказа</div>
            <div class="card-body p-0">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Товар</th>
                            <th class="text-center">Кол-во</th>
                            <th class="text-end">Сумма</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var line in order.Lines)
                        {
                            <tr>
                                <td>@line.Product.Name</td>
                                <td class="text-center">@line.Quantity</td>
                                <td class="text-end">@((line.Product.Price * line.Quantity).ToString("c"))</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card checkout-card mb-3">
            <div class="card-header"><i class="fas fa-map-marker-alt me-1"></i> Адрес доставки</div>
            <div class="card-body">
                <p class="mb-1"><b>Получатель:</b> @order.Name (@order.Phone)</p>
                <p class="mb-1"><b>Email:</b> @order.Email</p>
                <p class="mb-1">
                    <b>Адрес:</b>
                    @order.Country, @order.PostalCode, @order.City, ул. @order.Street, д. @order.Building
                    @if (!string.IsNullOrEmpty(order.Apartment))
                    {
                        <span>, кв. @order.Apartment</span>
                    }
                </p>
                <p class="mb-0"><b>Доставка:</b> @delivery</p>
                @if (!string.IsNullOrEmpty(order.Comment))
                {
                    <p class="mb-0 mt-2"><b>Комментарий:</b> @order.Comment</p>
                }
            </div>
        </div>

        <div class="card checkout-card">
            <div class="card-body">
                <div class="d-flex justify-content-between mb-1">
                    <span>Подытог</span>
                    <span>@((order.TotalAmount - order.ShippingCost).ToString("c"))</span>
                </div>
                <div class="d-flex justify-content-between mb-1">
                    <span>Доставка</span>
                    <span>@order.ShippingCost.ToString("c")</span>
                </div>
                <hr />
                <div class="d-flex justify-content-between fw-bold fs-5">
                    <span>Итого</span>
                    <span>@order.TotalAmount.ToString("c")</span>
                </div>
            </div>
            <div class="card-footer bg-white">
                <a class="btn btn-primary w-100" asp-controller="Home" asp-action="Index">
                    <i class="fas fa-store me-1"></i> Вернуться в магазин
                </a>
            </div>
        </div>
    </div>
}
